import { StyledContentContainer } from "./Containers.styles";

export const PageContentContainer = ({ children }: { children: any }) => {
  return <StyledContentContainer>{children}</StyledContentContainer>;
};
